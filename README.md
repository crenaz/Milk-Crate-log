#Milk-Crate-Log

This is a medicine tracking Log for my cat Milk-Crate, who has Chronic kidney disease (CKD)

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. or `pnpm i`
3. Run the app:
   `npm run dev`
